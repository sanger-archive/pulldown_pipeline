<div class="tube">
  <div class="tube-view">
    <%= render :partial => 'lab_ware/aliquot', :locals => { :lab_ware => tube, :id => 'tube', :container => tube } %>
  </div>

  <ul id="information">
    <li><span class="label">Barcode</span>       <%= useful_barcode(tube.barcode) %></li>
    <li><span class="label">Current state</span> <%= tube.state %></li>
  </ul>
</div>
